services:
  postgres:
    container_name: penny-pilot-postgres
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Urekmazino
      - POSTGRES_DB=penny-pilot
    ports:
      - "5432:5432"
    volumes:
      - ./postgres/init:/docker-entrypoint-initdb.d
